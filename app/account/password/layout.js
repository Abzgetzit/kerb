export const metadata = {
  title: "Change Password | Kerb Car",
  description: "Change the password for your Kerb Car account.",
};

export default function AccountPasswordLayout({ children }) {
  return children;
}
